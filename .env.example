# Environment Configuration Example
# Copy this file to .env and fill in your values

# OpenAI Configuration (required)
OPENAI_API_KEY=your-api-key-here
KEY_OWNER=Your Name

# Django Configuration
# Generate a secret key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
# Note: For local development, this can be omitted (a random key is generated)
DJANGO_SECRET_KEY=your-secret-key-here

# Django debug mode (optional, defaults to False)
# Set to "true" only for development
DJANGO_DEBUG=False

# Allowed hosts (optional, defaults to localhost,127.0.0.1)
# Comma-separated list of hostnames
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Model Configuration (optional)
# Preset options: performance, balanced (default), economy
# MODEL_PRESET=balanced

# Individual model overrides (optional)
# MODEL_PERCEIVE=gpt-5-mini
# MODEL_RETRIEVE_EMBEDDING=text-embedding-3-large
# MODEL_PLAN=gpt-5
# MODEL_REFLECT=gpt-5
# MODEL_EXECUTE=gpt-5-mini
# MODEL_CONVERSE=gpt-5
